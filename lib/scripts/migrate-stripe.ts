export const fetchStripeDonations = async () => {
    return
}

export const formatStripeDonations = async () => {
    return
}

export const uploadDonation = async () => {
    return
}

export const fetchDonation = async () => {
    return
}

// fetch all donations from old db

// fetch all stripe donations not included in old db

// for each, construct args with fallbacks

// construct new obj

// insert
